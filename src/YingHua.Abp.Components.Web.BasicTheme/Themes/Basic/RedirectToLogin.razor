@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        System.Console.WriteLine("Ìø×ªµÇÂ¼");
        Navigation.NavigateTo($"authentication/login?returnUrl={Uri.EscapeDataString(Navigation.Uri)}");
    }
}
